---

# Front Matter (YAML)

author: "Sebastien Rousseau"
banner_alt: "ตึกสีขาวกับหน้าต่างสีดำ"
banner_height: "100vh"
banner_width: "100vw"
banner: "https://kura.pro/stock/images/banners/daniele-franchi-Vl6YuVBLEys.webp"
cdn: "https://cloudcdn.pro"
changefreq: "weekly"
charset: "utf-8"
cname: ""
copyright: "© 2023-2026 ตัวแยกวิเคราะห์ใบแจ้งยอดบัญชีธนาคาร สงวนลิขสิทธิ์."
date: "Apr 01, 2026"
description: "เริ่มต้นใช้งาน Bank Statement Parser สำหรับ Python: ติดตั้ง แยกวิเคราะห์ไฟล์ CAMT/PAIN.001/CSV/OFX/QFX/MT940 และใช้เวิร์กโฟลว์การสตรีมหรือ CLI"
download: ""
format-detection: "telephone=no"
hreflang: "th"
icon: "https://cloudcdn.pro/bankstatementparser/images/favicon.ico"
id: "https://bankstatementparser.com/th/premiers-pas/index.html"
image_alt: "โลโก้ของ Bank Statement Parser ซึ่งเป็นเครื่องมือ Python อันทรงพลังที่ออกแบบมาเพื่อการประมวลผลข้อมูลทางการเงินและการดึงข้อมูลเชิงลึกที่รวดเร็วและแม่นยำ"
image_height: "630"
image_width: "1200"
image: "https://cloudcdn.pro/bankstatementparser/images/logos/bankstatementparser.webp"
keywords: "ตัวแยกวิเคราะห์ใบแจ้งยอดธนาคาร, การเริ่มต้น, หลาม, CAMT, PAIN.001, CSV, OFX, QFX, MT940, ข้อมูลทางการเงิน"
language: "th-TH"
layout: "start"
locale: "th_TH"
logo_alt: "โลโก้ของ Bank Statement Parser ซึ่งเป็นเครื่องมือ Python อันทรงพลังที่ออกแบบมาเพื่อการประมวลผลข้อมูลทางการเงินและการดึงข้อมูลเชิงลึกที่รวดเร็วและแม่นยำ"
logo_height: "44"
logo_width: "44"
logo: "https://cloudcdn.pro/bankstatementparser/images/logos/bankstatementparser.webp"
menu: "active"
measurementID: "G-FL9DEBFHN1"
name: "เริ่มต้นใช้งาน"
permalink: "https://bankstatementparser.com/th/premiers-pas/index.html"
rating: "general"
referrer: "no-referrer"
revisit-after: "7 days"
robots: "index, follow"
short_name: "bankstatementparser"
subtitle: "เริ่มสร้างแอปพลิเคชันที่ปลอดภัยด้วย Bank Statement Parser"
tags: "ธนาคาร,คำสั่ง,parser,python,camt,pain001,csv,ofx,qfx,mt940,สตรีมมิ่ง,cli"
theme_color: "rgb(73, 214, 251)"
title: "ตัวแยกวิเคราะห์ใบแจ้งยอดธนาคาร: คู่มือการติดตั้งและการใช้งาน"
url: "https://bankstatementparser.com/th/premiers-pas/index.html"
viewport: "width=device-width, initial-scale=1, shrink-to-fit=no"

# RSS - The RSS feed front matter (YAML).

atom_link: "https://bankstatementparser.com/th/premiers-pas/rss.xml"
category: "ซอฟต์แวร์ทางการเงิน, ไลบรารี Python, คู่มือนักพัฒนา"
docs: "https://validator.w3.org/feed/docs/rss2.html"
generator: "Shokunin 🦀 (version 0.0.20)"
item_description: "เริ่มต้นใช้งาน Bank Statement Parser สำหรับ Python: ติดตั้ง แยกวิเคราะห์ไฟล์ CAMT/PAIN.001/CSV/OFX/QFX/MT940 และใช้เวิร์กโฟลว์การสตรีมหรือ CLI"
item_guid: "https://bankstatementparser.com/th/premiers-pas/rss.xml"
item_link: "https://bankstatementparser.com/th/premiers-pas/rss.xml"
item_pub_date: "2026-04-01T00:00:00+00:00"
item_title: "ตัวแยกวิเคราะห์ใบแจ้งยอดธนาคาร: คู่มือการติดตั้งและการใช้งาน"
last_build_date: "2026-04-01T00:00:00+00:00"
managing_editor: "contact@bankstatementparser.com"
pub_date: "2026-04-01T00:00:00+00:00"
ttl: "60"
type: "website"
webmaster: "contact@bankstatementparser.com"

# Apple - The Apple front matter (YAML).

apple_mobile_web_app_orientations: "portrait"
apple_touch_icon_sizes: "192x192"
apple-mobile-web-app-capable: "yes"
mobile-web-app-capable: "yes"
apple-mobile-web-app-status-bar-inset: "black"
apple-mobile-web-app-status-bar-style: "black-translucent"
apple-mobile-web-app-title: "ตัวแยกวิเคราะห์ใบแจ้งยอดธนาคาร: คู่มือการติดตั้งและการใช้งาน"
apple-touch-fullscreen: "yes"

# MS Application - The MS Application front matter (YAML).

msapplication-config: "https://bankstatementparser.com/browserconfig.xml"
msapplication-tap-highlight: "no"
msapplication-TileColor: "rgb(73, 214, 251)"
msapplication_tile_image: "https://cloudcdn.pro/bankstatementparser/images/logos/bankstatementparser.webp"

# Twitter Card - The Twitter Card front matter (YAML).

twitter_card: "summary_large_image"
twitter_creator: "@wwdseb"
twitter_description: "ติดตั้งและใช้ Bank Statement Parser เพื่อแยกวิเคราะห์ไฟล์ CAMT, PAIN.001, CSV, OFX/QFX และ MT940 ใน Python"
twitter_image: "https://cloudcdn.pro/bankstatementparser/images/logos/bankstatementparser.webp"
twitter_image_alt: "โลโก้ของ Bank Statement Parser ซึ่งเป็นเครื่องมือ Python อันทรงพลังที่ออกแบบมาเพื่อการประมวลผลข้อมูลทางการเงินและการดึงข้อมูลเชิงลึกที่รวดเร็วและแม่นยำ"
twitter_site: "@wwdseb"
twitter_title: "ตัวแยกวิเคราะห์ใบแจ้งยอดธนาคาร: คู่มือการติดตั้งและการใช้งาน"
twitter_url: "https://bankstatementparser.com/th/premiers-pas/index.html"

# Humans.txt - The Humans.txt front matter (YAML).

author_website: "https://bankstatementparser.com"
author_twitter: "@wwdseb"
author_location: "London, UK"
thanks: "ขอบคุณสำหรับการอ่าน!"
site_last_updated: "2026-04-01"
site_standards: "HTML5, CSS3, RSS, Atom, JSON, XML, YAML, Markdown, TOML"
site_components: "Shokunin SSG, Shokunin CLI, Shokunin Templates, Shokunin Themes, Kaishi SSG, Kaishi CLI, Kaishi Templates, Kaishi Themes"
site_software: "Shokunin, Rust"

---

## ความต้องการ

- ไพธอน 3.9 ถึง 3.14
- การเข้าถึงเทอร์มินัล (macOS, Linux หรือ WSL)

## ติดตั้ง

```bash
pip install bankstatementparser
```

สำหรับการสนับสนุน Polars DataFrame:

```bash
pip install bankstatementparser[polars]
```

## เริ่มต้นอย่างรวดเร็ว

### ตรวจจับอัตโนมัติและแยกวิเคราะห์รูปแบบใด ๆ

```python
from bankstatementparser import create_parser, detect_statement_format

fmt = detect_statement_format("transactions.ofx")
parser = create_parser("transactions.ofx", fmt)
df = parser.parse()  # pandas DataFrame
print(df.head())
```

วิธีนี้ใช้ได้กับ`.xml`(CAMT/PAIN.001)`.csv`, `.ofx`, `.qfx`, `.mt940`, และ`.sta`ไฟล์.

### แยกวิเคราะห์ CAMT.053

```python
from bankstatementparser import CamtParser

parser = CamtParser("statement.xml")
transactions = parser.parse()
```

### แยกวิเคราะห์ PAIN.001

```python
from bankstatementparser import Pain001Parser

parser = Pain001Parser("payment.xml")
payments = parser.parse()
```

## สตรีมมิ่งไฟล์ขนาดใหญ่

สำหรับไฟล์ที่มีธุรกรรมหลายพันรายการ ให้ใช้การสตรีมเพื่อจำกัดขอบเขตหน่วยความจำ:

```python
parser = CamtParser("large_statement.xml")
for transaction in parser.parse_streaming(redact_pii=True):
    process(transaction)  # Memory stays constant
```

## การแยกวิเคราะห์ในหน่วยความจำ

แยกวิเคราะห์จากไบต์ที่ไม่มีดิสก์ I/O -- มีประโยชน์สำหรับเวิร์กโฟลว์ SFTP หรือ API:

```python
xml_bytes = download_from_sftp()
parser = CamtParser.from_bytes(xml_bytes, source_name="daily.xml")
transactions = parser.parse()
```

## การประมวลผลไฟล์แบบขนาน

แยกวิเคราะห์หลายไฟล์พร้อมกัน:

```python
from bankstatementparser import parse_files_parallel

results = parse_files_parallel([
    "statements/jan.xml",
    "statements/feb.xml",
    "statements/mar.xml",
])
for r in results:
    print(r.path, r.status, len(r.transactions), "rows")
```

## การขจัดข้อมูลซ้ำซ้อน

ตรวจจับรายการที่ซ้ำกันทุกประการและการแข่งขันที่ต้องสงสัยด้วยคะแนนความมั่นใจ:

```python
from bankstatementparser import CamtParser, Deduplicator

parser = CamtParser("statement.xml")
dedup = Deduplicator()
result = dedup.deduplicate(dedup.from_dataframe(parser.parse()))

print(f"Unique: {len(result.unique_transactions)}")
print(f"Exact duplicates: {len(result.exact_duplicates)}")
print(f"Suspected matches: {len(result.suspected_matches)}")
```

## การประมวลผล ZIP ที่ปลอดภัย

ประมวลผลไฟล์ XML แบบซิปพร้อมการตรวจสอบความปลอดภัยในตัว (การป้องกันระเบิด การปฏิเสธรายการที่เข้ารหัส):

```python
from bankstatementparser import iter_secure_xml_entries, CamtParser

for entry in iter_secure_xml_entries("statements.zip"):
    parser = CamtParser.from_bytes(entry.xml_bytes, source_name=entry.source_name)
    print(f"{entry.source_name}: {len(parser.parse())} transactions")
```

## ส่งออก

```python
parser = CamtParser("statement.xml")
parser.export_csv("output.csv")
parser.export_json("output.json")

# Polars (requires bankstatementparser[polars])
polars_df = parser.to_polars()
```

## การใช้งาน CLI

```bash
# Parse and display
python -m bankstatementparser.cli --type camt --input statement.xml

# Export to CSV
python -m bankstatementparser.cli --type camt --input statement.xml --output transactions.csv

# Stream with PII visible
python -m bankstatementparser.cli --type camt --input statement.xml --streaming --show-pii
```

ตัวเลือก CLI:

- `--type {camt,pain001}`-- ประเภทพาร์เซอร์
-`--input <path>`-- ไฟล์อินพุต
-`--output <csv_path>`- ส่งออกเป็น CSV
-`--streaming`-- สตรีมไฟล์ขนาดใหญ่
-`--show-pii`-- แสดงช่องที่ละเอียดอ่อน (แก้ไขโดยค่าเริ่มต้น)
-`--max-size <MB>`-- ขีดจำกัดขนาดไฟล์

## การตั้งค่าการพัฒนาท้องถิ่น

```bash
git clone https://github.com/sebastienrousseau/bankstatementparser.git
cd bankstatementparser
python3 -m venv .venv && source .venv/bin/activate
pip install poetry && poetry install --with dev
```

เรียกใช้ชุดทดสอบ:

```bash
pytest
```

## การอ้างอิง API

### คลาส Parser

| ระดับ | รูปแบบ | นำเข้า |
|---|---|---|
| `CamtParser` | CAMT.053 (ISO 20022) | `from bankstatementparser import CamtParser` |
| `Pain001Parser` | ความเจ็บปวด.001 (ISO 20022) | `from bankstatementparser import Pain001Parser` |
| `CsvStatementParser` | ซีเอสวี | `from bankstatementparser import CsvStatementParser` |
| `OfxParser` | โอเอฟเอ็กซ์ | `from bankstatementparser import OfxParser` |
| `QfxParser` | คิวเอฟเอ็กซ์ | `from bankstatementparser import QfxParser` |
| `Mt940Parser` | MT940 | `from bankstatementparser import Mt940Parser` |

### ฟังก์ชั่นยูทิลิตี้

| การทำงาน | วัตถุประสงค์ |
|---|---|
| `detect_statement_format(path)` | ตรวจจับรูปแบบไฟล์อัตโนมัติ |
| `create_parser(path, fmt)` | สร้างตัวแยกวิเคราะห์ที่เหมาะสม |
| `parse_files_parallel(paths)` | แยกวิเคราะห์หลายไฟล์พร้อมกัน |
| `iter_secure_xml_entries(zip_path)` | ทำซ้ำรายการ ZIP อย่างปลอดภัย |

### คลาสข้อมูล

| ระดับ | วัตถุประสงค์ |
|---|---|
| `Deduplicator` | ตรวจจับธุรกรรมที่ซ้ำกัน |
| `DeduplicationResult` | ผลลัพธ์ที่มีการจับคู่ที่ไม่ซ้ำ ตรงทั้งหมด และต้องสงสัย |
| `InputValidator` | ตรวจสอบเส้นทางและรูปแบบไฟล์ |
| `Transaction` | บันทึกธุรกรรมที่เป็นมาตรฐาน |
| `FileResult` | ผลลัพธ์จากการแยกวิเคราะห์แบบขนาน |
| `ZipXMLSource` | กระดาษห่อสมาชิก ZIP |

### ข้อยกเว้น

| ข้อยกเว้น | เมื่อถูกยกขึ้น |
|---|---|
| `ParserError` | การแยกวิเคราะห์ความล้มเหลว |
| `ExportError` | การส่งออกล้มเหลว (CSV/JSON/Excel) |
| `ValidationError` | การตรวจสอบอินพุตล้มเหลว |
| `ZipSecurityError` | ความล้มเหลวในการตรวจสอบความปลอดภัยของ ZIP |
