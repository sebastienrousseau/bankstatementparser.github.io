---

# Front Matter (YAML)

author: "Sebastien Rousseau"
banner_alt: "Tòa nhà màu trắng với cửa sổ màu đen"
banner_height: "100vh"
banner_width: "100vw"
banner: "https://kura.pro/stock/images/banners/daniele-franchi-Vl6YuVBLEys.webp"
cdn: ""
changefreq: "weekly"
charset: "utf-8"
cname: ""
copyright: "© 2023-2026 Trình phân tích báo cáo ngân hàng. Mọi quyền được bảo lưu."
date: "Apr 01, 2026"
description: "Bắt đầu với Trình phân tích bảng kê ngân hàng cho Python: cài đặt, phân tích các tệp CAMT/PAIN.001/CSV/OFX/QFX/MT940 và sử dụng quy trình phát trực tuyến hoặc CLI."
download: ""
format-detection: "telephone=no"
hreflang: "vi"
icon: "/images/favicon.ico"
id: "https://bankstatementparser.com/vi/premiers-pas/index.html"
image_alt: "Logo của Bank Statement Parser, một công cụ Python mạnh mẽ được thiết kế để xử lý dữ liệu tài chính nhanh chóng, chính xác và trích xuất thông tin chi tiết."
image_height: "630"
image_width: "1200"
image: "/images/logos/bankstatementparser.webp"
keywords: "trình phân tích cú pháp bảng sao kê ngân hàng, bắt đầu, python, CAMT, PAIN.001, CSV, OFX, QFX, MT940, dữ liệu tài chính"
language: "vi-VN"
layout: "start"
locale: "vi_VN"
logo_alt: "Logo của Bank Statement Parser, một công cụ Python mạnh mẽ được thiết kế để xử lý dữ liệu tài chính nhanh chóng, chính xác và trích xuất thông tin chi tiết."
logo_height: "44"
logo_width: "44"
logo: "/images/logos/bankstatementparser.webp"
menu: "active"
measurementID: "G-FL9DEBFHN1"
name: "Bắt đầu"
permalink: "https://bankstatementparser.com/vi/premiers-pas/index.html"
rating: "general"
referrer: "no-referrer"
revisit-after: "7 days"
robots: "index, follow"
short_name: "bankstatementparser"
subtitle: "Bắt đầu xây dựng các ứng dụng an toàn với Trình phân tích bảng sao kê ngân hàng"
tags: "ngân hàng,tuyên bố,trình phân tích cú pháp,python,camt,pain001,csv,ofx,qfx,mt940,phát trực tuyến,cli"
theme_color: "rgb(73, 214, 251)"
title: "Trình phân tích sao kê ngân hàng: Hướng dẫn cài đặt và sử dụng"
url: "https://bankstatementparser.com/vi/premiers-pas/index.html"
viewport: "width=device-width, initial-scale=1, shrink-to-fit=no"

# RSS - The RSS feed front matter (YAML).

atom_link: "https://bankstatementparser.com/vi/premiers-pas/rss.xml"
category: "Phần mềm tài chính, Thư viện Python, Hướng dẫn dành cho nhà phát triển"
docs: "https://validator.w3.org/feed/docs/rss2.html"
generator: "Shokunin 🦀 (version 0.0.20)"
item_description: "Bắt đầu với Trình phân tích bảng kê ngân hàng cho Python: cài đặt, phân tích các tệp CAMT/PAIN.001/CSV/OFX/QFX/MT940 và sử dụng quy trình phát trực tuyến hoặc CLI."
item_guid: "https://bankstatementparser.com/vi/premiers-pas/rss.xml"
item_link: "https://bankstatementparser.com/vi/premiers-pas/rss.xml"
item_pub_date: "2026-04-01T00:00:00+00:00"
item_title: "Trình phân tích sao kê ngân hàng: Hướng dẫn cài đặt và sử dụng"
last_build_date: "2026-04-01T00:00:00+00:00"
managing_editor: "contact@bankstatementparser.com"
pub_date: "2026-04-01T00:00:00+00:00"
ttl: "60"
type: "website"
webmaster: "contact@bankstatementparser.com"

# Apple - The Apple front matter (YAML).

apple_mobile_web_app_orientations: "portrait"
apple_touch_icon_sizes: "192x192"
apple-mobile-web-app-capable: "yes"
mobile-web-app-capable: "yes"
apple-mobile-web-app-status-bar-inset: "black"
apple-mobile-web-app-status-bar-style: "black-translucent"
apple-mobile-web-app-title: "Trình phân tích sao kê ngân hàng: Hướng dẫn cài đặt và sử dụng"
apple-touch-fullscreen: "yes"

# MS Application - The MS Application front matter (YAML).

msapplication-config: "https://bankstatementparser.com/browserconfig.xml"
msapplication-tap-highlight: "no"
msapplication-TileColor: "rgb(73, 214, 251)"
msapplication_tile_image: "/images/logos/bankstatementparser.webp"

# Twitter Card - The Twitter Card front matter (YAML).

twitter_card: "summary_large_image"
twitter_creator: "@wwdseb"
twitter_description: "Cài đặt và sử dụng Trình phân tích bảng sao kê ngân hàng để phân tích các tệp CAMT, PAIN.001, CSV, OFX/QFX và MT940 bằng Python."
twitter_image: "/images/logos/bankstatementparser.webp"
twitter_image_alt: "Logo của Bank Statement Parser, một công cụ Python mạnh mẽ được thiết kế để xử lý dữ liệu tài chính nhanh chóng, chính xác và trích xuất thông tin chi tiết."
twitter_site: "@wwdseb"
twitter_title: "Trình phân tích sao kê ngân hàng: Hướng dẫn cài đặt và sử dụng"
twitter_url: "https://bankstatementparser.com/vi/premiers-pas/index.html"

# Humans.txt - The Humans.txt front matter (YAML).

author_website: "https://bankstatementparser.com"
author_twitter: "@wwdseb"
author_location: "London, UK"
thanks: "Cảm ơn đã đọc!"
site_last_updated: "2026-04-01"
site_standards: "HTML5, CSS3, RSS, Atom, JSON, XML, YAML, Markdown, TOML"
site_components: "Shokunin SSG, Shokunin CLI, Shokunin Templates, Shokunin Themes, Kaishi SSG, Kaishi CLI, Kaishi Templates, Kaishi Themes"
site_software: "Shokunin, Rust"

---

## Yêu cầu

- Python 3,9 đến 3,14
- Truy cập thiết bị đầu cuối (macOS, Linux hoặc WSL)

## Cài đặt

```bash
pip install bankstatementparser
```

Để được hỗ trợ Polars DataFrame:

```bash
pip install bankstatementparser[polars]
```

## Bắt đầu nhanh

### Tự động phát hiện và phân tích mọi định dạng

```python
from bankstatementparser import create_parser, detect_statement_format

fmt = detect_statement_format("transactions.ofx")
parser = create_parser("transactions.ofx", fmt)
df = parser.parse()  # pandas DataFrame
print(df.head())
```

Điều này hoạt động với`.xml`(CAMT/PAIN.001),`.csv`, `.ofx`, `.qfx`, `.mt940`, Và`.sta`tập tin.

### Phân tích CAMT.053

```python
from bankstatementparser import CamtParser

parser = CamtParser("statement.xml")
transactions = parser.parse()
```

### Phân tích PAIN.001

```python
from bankstatementparser import Pain001Parser

parser = Pain001Parser("payment.xml")
payments = parser.parse()
```

## Truyền phát các tệp lớn

Đối với các tệp có hàng nghìn giao dịch, hãy sử dụng tính năng phát trực tuyến để giữ giới hạn bộ nhớ:

```python
parser = CamtParser("large_statement.xml")
for transaction in parser.parse_streaming(redact_pii=True):
    process(transaction)  # Memory stays constant
```

## Phân tích cú pháp trong bộ nhớ

Phân tích cú pháp từ byte mà không cần I/O đĩa -- hữu ích cho quy trình làm việc SFTP hoặc API:

```python
xml_bytes = download_from_sftp()
parser = CamtParser.from_bytes(xml_bytes, source_name="daily.xml")
transactions = parser.parse()
```

## Xử lý tệp song song

Phân tích đồng thời nhiều tệp:

```python
from bankstatementparser import parse_files_parallel

results = parse_files_parallel([
    "statements/jan.xml",
    "statements/feb.xml",
    "statements/mar.xml",
])
for r in results:
    print(r.path, r.status, len(r.transactions), "rows")
```

## Chống trùng lặp

Phát hiện các bản sao chính xác và các kết quả trùng khớp đáng ngờ bằng điểm tin cậy:

```python
from bankstatementparser import CamtParser, Deduplicator

parser = CamtParser("statement.xml")
dedup = Deduplicator()
result = dedup.deduplicate(dedup.from_dataframe(parser.parse()))

print(f"Unique: {len(result.unique_transactions)}")
print(f"Exact duplicates: {len(result.exact_duplicates)}")
print(f"Suspected matches: {len(result.suspected_matches)}")
```

## Xử lý ZIP an toàn

Xử lý các tệp XML đã nén bằng tính năng kiểm tra bảo mật tích hợp (bảo vệ chống bom, từ chối mục nhập được mã hóa):

```python
from bankstatementparser import iter_secure_xml_entries, CamtParser

for entry in iter_secure_xml_entries("statements.zip"):
    parser = CamtParser.from_bytes(entry.xml_bytes, source_name=entry.source_name)
    print(f"{entry.source_name}: {len(parser.parse())} transactions")
```

## Xuất khẩu

```python
parser = CamtParser("statement.xml")
parser.export_csv("output.csv")
parser.export_json("output.json")

# Polars (requires bankstatementparser[polars])
polars_df = parser.to_polars()
```

## Cách sử dụng CLI

```bash
# Parse and display
python -m bankstatementparser.cli --type camt --input statement.xml

# Export to CSV
python -m bankstatementparser.cli --type camt --input statement.xml --output transactions.csv

# Stream with PII visible
python -m bankstatementparser.cli --type camt --input statement.xml --streaming --show-pii
```

Tùy chọn CLI:

- `--type {camt,pain001}`-- loại trình phân tích cú pháp
-`--input <path>`-- tập tin đầu vào
-`--output <csv_path>`-- xuất sang CSV
-`--streaming`-- truyền phát các tập tin lớn
-`--show-pii`-- hiển thị các trường nhạy cảm (được xử lý lại theo mặc định)
-`--max-size <MB>`-- giới hạn kích thước tập tin

## Thiết lập phát triển cục bộ

```bash
git clone https://github.com/sebastienrousseau/bankstatementparser.git
cd bankstatementparser
python3 -m venv .venv && source .venv/bin/activate
pip install poetry && poetry install --with dev
```

Chạy bộ thử nghiệm:

```bash
pytest
```

## Tham khảo API

### Lớp phân tích cú pháp

| Lớp học | Định dạng | Nhập khẩu |
|---|---|---|
| `CamtParser` | CAMT.053 (ISO 20022) | `from bankstatementparser import CamtParser` |
| `Pain001Parser` | PAIN.001 (ISO 20022) | `from bankstatementparser import Pain001Parser` |
| `CsvStatementParser` | CSV | `from bankstatementparser import CsvStatementParser` |
| `OfxParser` | OFX | `from bankstatementparser import OfxParser` |
| `QfxParser` | QFX | `from bankstatementparser import QfxParser` |
| `Mt940Parser` | MT940 | `from bankstatementparser import Mt940Parser` |

### Chức năng tiện ích

| Chức năng | Mục đích |
|---|---|
| `detect_statement_format(path)` | Tự động phát hiện định dạng tập tin |
| `create_parser(path, fmt)` | Tạo trình phân tích cú pháp thích hợp |
| `parse_files_parallel(paths)` | Phân tích đồng thời nhiều tệp |
| `iter_secure_xml_entries(zip_path)` | Lặp lại các mục ZIP một cách an toàn |

### Lớp dữ liệu

| Lớp học | Mục đích |
|---|---|
| `Deduplicator` | Phát hiện giao dịch trùng lặp |
| `DeduplicationResult` | Kết quả có kết quả trùng khớp duy nhất, chính xác và đáng ngờ |
| `InputValidator` | Xác thực đường dẫn và định dạng tệp |
| `Transaction` | Hồ sơ giao dịch chuẩn hóa |
| `FileResult` | Kết quả phân tích song song |
| `ZipXMLSource` | Trình bao bọc thành viên ZIP |

### Ngoại lệ

| Ngoại lệ | Khi nâng lên |
|---|---|
| `ParserError` | Lỗi phân tích cú pháp |
| `ExportError` | Lỗi xuất (CSV/JSON/Excel) |
| `ValidationError` | Lỗi xác thực đầu vào |
| `ZipSecurityError` | Lỗi kiểm tra bảo mật ZIP |
