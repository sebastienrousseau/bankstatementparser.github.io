---

# Front Matter (YAML)

author: "Sebastien Rousseau"
banner_alt: "Ile funfun kan pẹlu awọn ferese dudu"
banner_height: "100vh"
banner_width: "100vw"
banner: "https://kura.pro/stock/images/banners/daniele-franchi-Vl6YuVBLEys.webp"
cdn: "https://cloudcdn.pro"
changefreq: "weekly"
charset: "utf-8"
cname: ""
copyright: "© 2023-2026 Itumọ Gbólóhùn Bank. Gbogbo awọn ẹtọ wa ni ipamọ."
date: "Apr 01, 2026"
description: "Bẹrẹ pẹlu Parser Gbólóhùn Bank fun Python: fi sori ẹrọ, parse CAMT/PAIN.001/CSV/OFX/QFX/MT940 awọn faili, ati lo ṣiṣanwọle tabi ṣiṣan iṣẹ CLI."
download: ""
format-detection: "telephone=no"
hreflang: "yo"
icon: "https://cloudcdn.pro/bankstatementparser/images/favicon.ico"
id: "https://bankstatementparser.com/yo/premiers-pas/index.html"
image_alt: "Logo ti Ikọju Gbólóhùn Banki, irinṣẹ Python ti o lagbara ti a ṣe apẹrẹ fun iyara, ṣiṣe data owo deede ati isediwon oye."
image_height: "630"
image_width: "1200"
image: "https://cloudcdn.pro/bankstatementparser/images/logos/bankstatementparser.webp"
keywords: "atunto alaye banki, bibẹrẹ, Python, CAMT, PAIN.001, CSV, OFX, QFX, MT940, data inawo"
language: "yo-NG"
layout: "start"
locale: "yo_NG"
logo_alt: "Logo ti Ikọju Gbólóhùn Banki, irinṣẹ Python ti o lagbara ti a ṣe apẹrẹ fun iyara, ṣiṣe data owo deede ati isediwon oye."
logo_height: "44"
logo_width: "44"
logo: "https://cloudcdn.pro/bankstatementparser/images/logos/bankstatementparser.webp"
menu: "active"
measurementID: "G-FL9DEBFHN1"
name: "Bibẹrẹ"
permalink: "https://bankstatementparser.com/yo/premiers-pas/index.html"
rating: "general"
referrer: "no-referrer"
revisit-after: "7 days"
robots: "index, follow"
short_name: "bankstatementparser"
subtitle: "Bẹrẹ Ilé Awọn ohun elo to ni aabo pẹlu Parser Gbólóhùn Bank"
tags: "banki, gbólóhùn, parser, Python, Camt, irora001, csv, ofx,qfx, mt940, sisanwọle,cli"
theme_color: "rgb(73, 214, 251)"
title: "Atọka Gbólóhùn Banki: Fifi sori ẹrọ ati Itọsọna Lilo"
url: "https://bankstatementparser.com/yo/premiers-pas/index.html"
viewport: "width=device-width, initial-scale=1, shrink-to-fit=no"

# RSS - The RSS feed front matter (YAML).

atom_link: "https://bankstatementparser.com/yo/premiers-pas/rss.xml"
category: "Owo Software, Python Library, Olùgbéejáde Itọsọna"
docs: "https://validator.w3.org/feed/docs/rss2.html"
generator: "Shokunin 🦀 (version 0.0.20)"
item_description: "Bẹrẹ pẹlu Parser Gbólóhùn Bank fun Python: fi sori ẹrọ, parse CAMT/PAIN.001/CSV/OFX/QFX/MT940 awọn faili, ati lo ṣiṣanwọle tabi ṣiṣan iṣẹ CLI."
item_guid: "https://bankstatementparser.com/yo/premiers-pas/rss.xml"
item_link: "https://bankstatementparser.com/yo/premiers-pas/rss.xml"
item_pub_date: "2026-04-01T00:00:00+00:00"
item_title: "Atọka Gbólóhùn Banki: Fifi sori ẹrọ ati Itọsọna Lilo"
last_build_date: "2026-04-01T00:00:00+00:00"
managing_editor: "contact@bankstatementparser.com"
pub_date: "2026-04-01T00:00:00+00:00"
ttl: "60"
type: "website"
webmaster: "contact@bankstatementparser.com"

# Apple - The Apple front matter (YAML).

apple_mobile_web_app_orientations: "portrait"
apple_touch_icon_sizes: "192x192"
apple-mobile-web-app-capable: "yes"
mobile-web-app-capable: "yes"
apple-mobile-web-app-status-bar-inset: "black"
apple-mobile-web-app-status-bar-style: "black-translucent"
apple-mobile-web-app-title: "Atọka Gbólóhùn Banki: Fifi sori ẹrọ ati Itọsọna Lilo"
apple-touch-fullscreen: "yes"

# MS Application - The MS Application front matter (YAML).

msapplication-config: "https://bankstatementparser.com/browserconfig.xml"
msapplication-tap-highlight: "no"
msapplication-TileColor: "rgb(73, 214, 251)"
msapplication_tile_image: "https://cloudcdn.pro/bankstatementparser/images/logos/bankstatementparser.webp"

# Twitter Card - The Twitter Card front matter (YAML).

twitter_card: "summary_large_image"
twitter_creator: "@wwdseb"
twitter_description: "Fi sori ẹrọ ati lo Parser Gbólóhùn Bank lati sọ CAMT, PAIN.001, CSV, OFX/QFX, ati awọn faili MT940 ni Python."
twitter_image: "https://cloudcdn.pro/bankstatementparser/images/logos/bankstatementparser.webp"
twitter_image_alt: "Logo ti Ikọju Gbólóhùn Banki, irinṣẹ Python ti o lagbara ti a ṣe apẹrẹ fun iyara, ṣiṣe data owo deede ati isediwon oye."
twitter_site: "@wwdseb"
twitter_title: "Atọka Gbólóhùn Banki: Fifi sori ẹrọ ati Itọsọna Lilo"
twitter_url: "https://bankstatementparser.com/yo/premiers-pas/index.html"

# Humans.txt - The Humans.txt front matter (YAML).

author_website: "https://bankstatementparser.com"
author_twitter: "@wwdseb"
author_location: "London, UK"
thanks: "O ṣeun fun kika!"
site_last_updated: "2026-04-01"
site_standards: "HTML5, CSS3, RSS, Atom, JSON, XML, YAML, Markdown, TOML"
site_components: "Shokunin SSG, Shokunin CLI, Shokunin Templates, Shokunin Themes, Kaishi SSG, Kaishi CLI, Kaishi Templates, Kaishi Themes"
site_software: "Shokunin, Rust"

---

## Awọn ibeere

- Python 3.9 si 3.14
- Wiwọle ebute (macOS, Linux, tabi WSL)

## Fi sori ẹrọ

```bash
pip install bankstatementparser
```

Fun atilẹyin Polars DataFrame:

```bash
pip install bankstatementparser[polars]
```

## Ibẹrẹ kiakia

### Ṣewadii-laifọwọyi ki o ṣe itupalẹ eyikeyi ọna kika

```python
from bankstatementparser import create_parser, detect_statement_format

fmt = detect_statement_format("transactions.ofx")
parser = create_parser("transactions.ofx", fmt)
df = parser.parse()  # pandas DataFrame
print(df.head())
```

Eyi ṣiṣẹ pẹlu`.xml`(CAMT/PAIN.001),`.csv`, `.ofx`, `.qfx`, `.mt940`, ati`.sta`awọn faili.

### Parse CAMT.053

```python
from bankstatementparser import CamtParser

parser = CamtParser("statement.xml")
transactions = parser.parse()
```

### ÌRÒYÌN ÌRÒYÌN.001

```python
from bankstatementparser import Pain001Parser

parser = Pain001Parser("payment.xml")
payments = parser.parse()
```

## Ṣiṣanwọle Awọn faili nla

Fun awọn faili pẹlu ẹgbẹẹgbẹrun awọn iṣowo, lo ṣiṣanwọle lati jẹ ki iranti di opin:

```python
parser = CamtParser("large_statement.xml")
for transaction in parser.parse_streaming(redact_pii=True):
    process(transaction)  # Memory stays constant
```

## Ni-Memory Parsing

Yatọ kuro ni awọn baiti laisi disiki I/O – wulo fun SFTP tabi awọn ṣiṣan iṣẹ API:

```python
xml_bytes = download_from_sftp()
parser = CamtParser.from_bytes(xml_bytes, source_name="daily.xml")
transactions = parser.parse()
```

## Iṣaṣe Faili ti o jọra

Pa ọpọlọpọ awọn faili ni igbakanna:

```python
from bankstatementparser import parse_files_parallel

results = parse_files_parallel([
    "statements/jan.xml",
    "statements/feb.xml",
    "statements/mar.xml",
])
for r in results:
    print(r.path, r.status, len(r.transactions), "rows")
```

## Deduplication

Ṣewadii awọn ẹda-ẹda deede ati awọn ere ti a fura si pẹlu awọn ikun igbekele:

```python
from bankstatementparser import CamtParser, Deduplicator

parser = CamtParser("statement.xml")
dedup = Deduplicator()
result = dedup.deduplicate(dedup.from_dataframe(parser.parse()))

print(f"Unique: {len(result.unique_transactions)}")
print(f"Exact duplicates: {len(result.exact_duplicates)}")
print(f"Suspected matches: {len(result.suspected_matches)}")
```

## Ṣiṣeto ZIP to ni aabo

Ṣiṣẹ awọn faili XML ti a fi sinu rẹ pẹlu awọn sọwedowo aabo ti a ṣe sinu (idaabobo bombu, ijusile titẹsi fifi ẹnọ kọ nkan):

```python
from bankstatementparser import iter_secure_xml_entries, CamtParser

for entry in iter_secure_xml_entries("statements.zip"):
    parser = CamtParser.from_bytes(entry.xml_bytes, source_name=entry.source_name)
    print(f"{entry.source_name}: {len(parser.parse())} transactions")
```

## Si ilẹ okeere

```python
parser = CamtParser("statement.xml")
parser.export_csv("output.csv")
parser.export_json("output.json")

# Polars (requires bankstatementparser[polars])
polars_df = parser.to_polars()
```

## CLI Lilo

```bash
# Parse and display
python -m bankstatementparser.cli --type camt --input statement.xml

# Export to CSV
python -m bankstatementparser.cli --type camt --input statement.xml --output transactions.csv

# Stream with PII visible
python -m bankstatementparser.cli --type camt --input statement.xml --streaming --show-pii
```

Awọn aṣayan CLI:

- `--type {camt,pain001}`-- iru parser
-`--input <path>`-- faili titẹ sii
-`--output <csv_path>`-- okeere si CSV
-`--streaming`- ṣiṣan awọn faili nla
-`--show-pii`- fihan awọn aaye ifarabalẹ (atunṣe nipasẹ aiyipada)
-`--max-size <MB>`-- opin iwọn faili

## Eto Idagbasoke Agbegbe

```bash
git clone https://github.com/sebastienrousseau/bankstatementparser.git
cd bankstatementparser
python3 -m venv .venv && source .venv/bin/activate
pip install poetry && poetry install --with dev
```

Ṣiṣe suite idanwo naa:

```bash
pytest
```

## API Itọkasi

### Awọn kilasi Parser

| Kilasi | Ọna kika | gbe wọle |
|---|---|---|
| `CamtParser` | CAMT.053 (ISO 20022) | `from bankstatementparser import CamtParser` |
| `Pain001Parser` | Ìrora.001 (ISO 20022) | `from bankstatementparser import Pain001Parser` |
| `CsvStatementParser` | CSV | `from bankstatementparser import CsvStatementParser` |
| `OfxParser` | OFX | `from bankstatementparser import OfxParser` |
| `QfxParser` | QFX | `from bankstatementparser import QfxParser` |
| `Mt940Parser` | MT940 | `from bankstatementparser import Mt940Parser` |

### Awọn iṣẹ IwUlO

| Išẹ | Idi |
|---|---|
| `detect_statement_format(path)` | Ṣe iwari ọna kika faili ni aifọwọyi |
| `create_parser(path, fmt)` | Ṣẹda parser ti o yẹ |
| `parse_files_parallel(paths)` | Pa awọn faili lọpọlọpọ nigbakanna |
| `iter_secure_xml_entries(zip_path)` | Ṣe atunto awọn titẹ sii ZIP ni aabo |

### Data Classes

| Kilasi | Idi |
|---|---|
| `Deduplicator` | Ṣawari awọn iṣowo ẹda-iwe |
| `DeduplicationResult` | Abajade pẹlu alailẹgbẹ, deede, ati awọn ibaamu ti a fura si |
| `InputValidator` | Sooto awọn ọna faili ati awọn ọna kika |
| `Transaction` | Igbasilẹ idunadura deede |
| `FileResult` | Abajade lati itọka ti o jọra |
| `ZipXMLSource` | Apoti egbe ZIP |

### Awọn imukuro

| Iyatọ | Nigbati Dide |
|---|---|
| `ParserError` | Awọn ikuna ṣiṣe ayẹwo |
| `ExportError` | Awọn ikuna okeere (CSV/JSON/Excel) |
| `ValidationError` | Awọn ikuna afọwọsi igbewọle |
| `ZipSecurityError` | Awọn ikuna ayẹwo aabo ZIP |
