---

# Front Matter (YAML)

author: "Sebastien Rousseau"
banner_alt: "কালো জানালা সহ একটি সাদা বিল্ডিং"
banner_height: "100vh"
banner_width: "100vw"
banner: "https://kura.pro/stock/images/banners/daniele-franchi-Vl6YuVBLEys.webp"
cdn: ""
changefreq: "weekly"
charset: "utf-8"
cname: ""
copyright: "© 2023-2026 ব্যাঙ্ক স্টেটমেন্ট পার্সার। সর্বস্বত্ব সংরক্ষিত"
date: "Apr 01, 2026"
description: "পাইথনের জন্য ব্যাঙ্ক স্টেটমেন্ট পার্সার দিয়ে শুরু করুন: CAMT/PAIN.001/CSV/OFX/QFX/MT940 ফাইলগুলি ইনস্টল করুন, পার্স করুন এবং স্ট্রিমিং বা CLI ওয়ার্কফ্লো ব্যবহার করুন৷"
download: ""
format-detection: "telephone=no"
hreflang: "bn"
icon: "/images/favicon.ico"
id: "https://bankstatementparser.com/bn/premiers-pas/index.html"
image_alt: "ব্যাঙ্ক স্টেটমেন্ট পার্সারের লোগো, একটি শক্তিশালী পাইথন টুল যা দ্রুত, সঠিক আর্থিক ডেটা প্রক্রিয়াকরণ এবং অন্তর্দৃষ্টি নিষ্কাশনের জন্য ডিজাইন করা হয়েছে।"
image_height: "630"
image_width: "1200"
image: "/images/logos/bankstatementparser.webp"
keywords: "ব্যাঙ্ক স্টেটমেন্ট পার্সার, শুরু করা, পাইথন, CAMT, PAIN.001, CSV, OFX, QFX, MT940, আর্থিক ডেটা"
language: "bn-BD"
layout: "start"
locale: "bn_BD"
logo_alt: "ব্যাঙ্ক স্টেটমেন্ট পার্সারের লোগো, একটি শক্তিশালী পাইথন টুল যা দ্রুত, সঠিক আর্থিক ডেটা প্রক্রিয়াকরণ এবং অন্তর্দৃষ্টি নিষ্কাশনের জন্য ডিজাইন করা হয়েছে।"
logo_height: "44"
logo_width: "44"
logo: "/images/logos/bankstatementparser.webp"
menu: "active"
measurementID: "G-FL9DEBFHN1"
name: "শুরু করা"
permalink: "https://bankstatementparser.com/bn/premiers-pas/index.html"
rating: "general"
referrer: "no-referrer"
revisit-after: "7 days"
robots: "index, follow"
short_name: "bankstatementparser"
subtitle: "ব্যাঙ্ক স্টেটমেন্ট পার্সার দিয়ে সুরক্ষিত অ্যাপ্লিকেশন তৈরি করা শুরু করুন"
tags: "ব্যাঙ্ক, স্টেটমেন্ট, পার্সার, পাইথন, camt,pain001, csv, ofx, qfx, mt940, স্ট্রিমিং, cli"
theme_color: "rgb(73, 214, 251)"
title: "ব্যাঙ্ক স্টেটমেন্ট পার্সার: ইনস্টলেশন এবং ব্যবহার নির্দেশিকা"
url: "https://bankstatementparser.com/bn/premiers-pas/index.html"
viewport: "width=device-width, initial-scale=1, shrink-to-fit=no"

# RSS - The RSS feed front matter (YAML).

atom_link: "https://bankstatementparser.com/bn/premiers-pas/rss.xml"
category: "ফাইন্যান্স সফটওয়্যার, পাইথন লাইব্রেরি, ডেভেলপার গাইড"
docs: "https://validator.w3.org/feed/docs/rss2.html"
generator: "Shokunin 🦀 (version 0.0.20)"
item_description: "পাইথনের জন্য ব্যাঙ্ক স্টেটমেন্ট পার্সার দিয়ে শুরু করুন: CAMT/PAIN.001/CSV/OFX/QFX/MT940 ফাইলগুলি ইনস্টল করুন, পার্স করুন এবং স্ট্রিমিং বা CLI ওয়ার্কফ্লো ব্যবহার করুন৷"
item_guid: "https://bankstatementparser.com/bn/premiers-pas/rss.xml"
item_link: "https://bankstatementparser.com/bn/premiers-pas/rss.xml"
item_pub_date: "2026-04-01T00:00:00+00:00"
item_title: "ব্যাঙ্ক স্টেটমেন্ট পার্সার: ইনস্টলেশন এবং ব্যবহার নির্দেশিকা"
last_build_date: "2026-04-01T00:00:00+00:00"
managing_editor: "contact@bankstatementparser.com"
pub_date: "2026-04-01T00:00:00+00:00"
ttl: "60"
type: "website"
webmaster: "contact@bankstatementparser.com"

# Apple - The Apple front matter (YAML).

apple_mobile_web_app_orientations: "portrait"
apple_touch_icon_sizes: "192x192"
apple-mobile-web-app-capable: "yes"
mobile-web-app-capable: "yes"
apple-mobile-web-app-status-bar-inset: "black"
apple-mobile-web-app-status-bar-style: "black-translucent"
apple-mobile-web-app-title: "ব্যাঙ্ক স্টেটমেন্ট পার্সার: ইনস্টলেশন এবং ব্যবহার নির্দেশিকা"
apple-touch-fullscreen: "yes"

# MS Application - The MS Application front matter (YAML).

msapplication-config: "https://bankstatementparser.com/browserconfig.xml"
msapplication-tap-highlight: "no"
msapplication-TileColor: "rgb(73, 214, 251)"
msapplication_tile_image: "/images/logos/bankstatementparser.webp"

# Twitter Card - The Twitter Card front matter (YAML).

twitter_card: "summary_large_image"
twitter_creator: "@wwdseb"
twitter_description: "পাইথনে CAMT, PAIN.001, CSV, OFX/QFX, এবং MT940 ফাইলগুলিকে পার্স করতে ব্যাঙ্ক স্টেটমেন্ট পার্সার ইনস্টল করুন এবং ব্যবহার করুন৷"
twitter_image: "/images/logos/bankstatementparser.webp"
twitter_image_alt: "ব্যাঙ্ক স্টেটমেন্ট পার্সারের লোগো, একটি শক্তিশালী পাইথন টুল যা দ্রুত, সঠিক আর্থিক ডেটা প্রক্রিয়াকরণ এবং অন্তর্দৃষ্টি নিষ্কাশনের জন্য ডিজাইন করা হয়েছে।"
twitter_site: "@wwdseb"
twitter_title: "ব্যাঙ্ক স্টেটমেন্ট পার্সার: ইনস্টলেশন এবং ব্যবহার নির্দেশিকা"
twitter_url: "https://bankstatementparser.com/bn/premiers-pas/index.html"

# Humans.txt - The Humans.txt front matter (YAML).

author_website: "https://bankstatementparser.com"
author_twitter: "@wwdseb"
author_location: "London, UK"
thanks: "পড়ার জন্য ধন্যবাদ!"
site_last_updated: "2026-04-01"
site_standards: "HTML5, CSS3, RSS, Atom, JSON, XML, YAML, Markdown, TOML"
site_components: "Shokunin SSG, Shokunin CLI, Shokunin Templates, Shokunin Themes, Kaishi SSG, Kaishi CLI, Kaishi Templates, Kaishi Themes"
site_software: "Shokunin, Rust"

---

## প্রয়োজনীয়তা

- পাইথন 3.9 থেকে 3.14
- টার্মিনাল অ্যাক্সেস (macOS, Linux, বা WSL)

## ইন্সটল করুন

```bash
pip install bankstatementparser
```

পোলার ডেটাফ্রেম সমর্থনের জন্য:

```bash
pip install bankstatementparser[polars]
```

## দ্রুত শুরু

### অটো-ডিটেক্ট এবং যেকোন ফরম্যাট পার্স করুন

```python
from bankstatementparser import create_parser, detect_statement_format

fmt = detect_statement_format("transactions.ofx")
parser = create_parser("transactions.ofx", fmt)
df = parser.parse()  # pandas DataFrame
print(df.head())
```

এই সঙ্গে কাজ করে`.xml`(CAMT/PAIN.001),`.csv`, `.ofx`, `.qfx`, `.mt940`, এবং`.sta`ফাইল

### CAMT.053 পার্স করুন

```python
from bankstatementparser import CamtParser

parser = CamtParser("statement.xml")
transactions = parser.parse()
```

### PAIN.001 পার্স করুন

```python
from bankstatementparser import Pain001Parser

parser = Pain001Parser("payment.xml")
payments = parser.parse()
```

## বড় ফাইল স্ট্রিমিং

হাজার হাজার লেনদেন সহ ফাইলগুলির জন্য, মেমরি আবদ্ধ রাখতে স্ট্রিমিং ব্যবহার করুন:

```python
parser = CamtParser("large_statement.xml")
for transaction in parser.parse_streaming(redact_pii=True):
    process(transaction)  # Memory stays constant
```

## ইন-মেমরি পার্সিং

ডিস্ক I/O ছাড়া বাইট থেকে পার্স করুন -- SFTP বা API কর্মপ্রবাহের জন্য দরকারী:

```python
xml_bytes = download_from_sftp()
parser = CamtParser.from_bytes(xml_bytes, source_name="daily.xml")
transactions = parser.parse()
```

## সমান্তরাল ফাইল প্রসেসিং

একসাথে একাধিক ফাইল পার্স করুন:

```python
from bankstatementparser import parse_files_parallel

results = parse_files_parallel([
    "statements/jan.xml",
    "statements/feb.xml",
    "statements/mar.xml",
])
for r in results:
    print(r.path, r.status, len(r.transactions), "rows")
```

## অনুলিপি

আত্মবিশ্বাসের স্কোর সহ সঠিক সদৃশ এবং সন্দেহজনক মিলগুলি সনাক্ত করুন:

```python
from bankstatementparser import CamtParser, Deduplicator

parser = CamtParser("statement.xml")
dedup = Deduplicator()
result = dedup.deduplicate(dedup.from_dataframe(parser.parse()))

print(f"Unique: {len(result.unique_transactions)}")
print(f"Exact duplicates: {len(result.exact_duplicates)}")
print(f"Suspected matches: {len(result.suspected_matches)}")
```

## নিরাপদ জিপ প্রক্রিয়াকরণ

বিল্ট-ইন নিরাপত্তা চেক (বোমা সুরক্ষা, এনক্রিপ্টেড এন্ট্রি প্রত্যাখ্যান) সহ জিপ করা XML ফাইলগুলি প্রক্রিয়া করুন:

```python
from bankstatementparser import iter_secure_xml_entries, CamtParser

for entry in iter_secure_xml_entries("statements.zip"):
    parser = CamtParser.from_bytes(entry.xml_bytes, source_name=entry.source_name)
    print(f"{entry.source_name}: {len(parser.parse())} transactions")
```

## রপ্তানি

```python
parser = CamtParser("statement.xml")
parser.export_csv("output.csv")
parser.export_json("output.json")

# Polars (requires bankstatementparser[polars])
polars_df = parser.to_polars()
```

## CLI ব্যবহার

```bash
# Parse and display
python -m bankstatementparser.cli --type camt --input statement.xml

# Export to CSV
python -m bankstatementparser.cli --type camt --input statement.xml --output transactions.csv

# Stream with PII visible
python -m bankstatementparser.cli --type camt --input statement.xml --streaming --show-pii
```

CLI বিকল্প:

- `--type {camt,pain001}`-- পার্সার টাইপ
-`--input <path>`-- ইনপুট ফাইল
-`--output <csv_path>`-- CSV এ রপ্তানি করুন
-`--streaming`-- বড় ফাইল স্ট্রিম করুন
-`--show-pii`-- সংবেদনশীল ক্ষেত্রগুলি দেখান (ডিফল্টরূপে সংশোধিত)
-`--max-size <MB>`-- ফাইল সাইজ সীমা

## স্থানীয় উন্নয়ন সেটআপ

```bash
git clone https://github.com/sebastienrousseau/bankstatementparser.git
cd bankstatementparser
python3 -m venv .venv && source .venv/bin/activate
pip install poetry && poetry install --with dev
```

পরীক্ষা স্যুট চালান:

```bash
pytest
```

## API রেফারেন্স

### পার্সার ক্লাস

| ক্লাস | বিন্যাস | আমদানি |
|---|---|---|
| `CamtParser` | CAMT.053 (ISO 20022) | `from bankstatementparser import CamtParser` |
| `Pain001Parser` | PAIN.001 (ISO 20022) | `from bankstatementparser import Pain001Parser` |
| `CsvStatementParser` | CSV | `from bankstatementparser import CsvStatementParser` |
| `OfxParser` | OFX | `from bankstatementparser import OfxParser` |
| `QfxParser` | QFX | `from bankstatementparser import QfxParser` |
| `Mt940Parser` | MT940 | `from bankstatementparser import Mt940Parser` |

### ইউটিলিটি ফাংশন

| ফাংশন | উদ্দেশ্য |
|---|---|
| `detect_statement_format(path)` | ফাইল বিন্যাস স্বয়ংক্রিয় সনাক্ত |
| `create_parser(path, fmt)` | উপযুক্ত পার্সার তৈরি করুন |
| `parse_files_parallel(paths)` | একযোগে একাধিক ফাইল পার্স করুন |
| `iter_secure_xml_entries(zip_path)` | নিরাপদে জিপ এন্ট্রি পুনরাবৃত্তি করুন |

### ডেটা ক্লাস

| ক্লাস | উদ্দেশ্য |
|---|---|
| `Deduplicator` | ডুপ্লিকেট লেনদেন সনাক্ত করুন |
| `DeduplicationResult` | অনন্য, সঠিক, এবং সন্দেহজনক মিলের ফলাফল |
| `InputValidator` | ফাইল পাথ এবং বিন্যাস যাচাই করুন |
| `Transaction` | স্বাভাবিক লেনদেনের রেকর্ড |
| `FileResult` | সমান্তরাল পার্সিং থেকে ফলাফল |
| `ZipXMLSource` | জিপ সদস্যের মোড়ক |

### ব্যতিক্রম

| ব্যতিক্রম | যখন উত্থাপিত |
|---|---|
| `ParserError` | পার্সিং ব্যর্থতা |
| `ExportError` | রপ্তানি ব্যর্থতা (CSV/JSON/Excel) |
| `ValidationError` | ইনপুট বৈধতা ব্যর্থতা |
| `ZipSecurityError` | জিপ নিরাপত্তা চেক ব্যর্থতা |
